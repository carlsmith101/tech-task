# tech task - Articles API endpoints

## Setup - install packages

Use the package manager [pip](https://pip.pypa.io/en/stable/) to install required packages.

```bash
pip install -r requirements.txt
```

## Run tests

```bash
python -m pytest
```